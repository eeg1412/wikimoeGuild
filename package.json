{
  "name": "wikimoeguildmaster",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start-server": "node ./bin/www",
    "build": "cd client && npm run build",
    "start": "npm run build && npm run start-server"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "morgan": "~1.9.1"
  }
}
